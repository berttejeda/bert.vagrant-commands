---
definitions:
  commands:
    foo:
      name: foo
      synopsis: Sets foo to the string bar
      execute:
          $foo = 'bar'
    bar:
      name: bar
      synopsis: Sets bar to the string baz
      execute:
          $bar = 'baz'
